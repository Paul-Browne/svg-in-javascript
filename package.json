{
  "name": "svg-in-javascript",
  "version": "1.0.2",
  "description": "SVG.JS is a library for easily creating SVG in JavaScript, for both the backend and the frontend.",
  "main": "esm.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js"
  },
  "author": "Paul Browne",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}
